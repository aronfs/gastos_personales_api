meta {
  name: Change Password
  type: http
  seq: 1
}

post {
  url: {{base_url}}/auth/change-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
{
  "currentPassword": "User123*",
  "newPassword": "NewUser123*"
}
}

